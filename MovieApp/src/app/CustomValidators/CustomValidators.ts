/*import { FormGroup, FormControl, Validator } from '@angular/forms';

export class CustomValidators implements Validator
{
    static passwordValidator(group: FormControl){
 if(group.controls != null){
        if(group.controls.cpassword.value  != group.controls.password.value){
            return {
                noMatch:true
            }

        }
 }
 return null;
    
    }
}*/